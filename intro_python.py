#!/usr/bin/python
# Write your code on the next line.
import sys
import os

def maxDifference(_a):

    return _a

f= open(os.environ['OUTPUT_PATH'], 'w')
_a_cnt =0
_a_cnt =6
_a_i = 0

_a =[]

while _a_i < _a_cnt:
    a_item = 10
    a.append(_a_item)
    a_i +=1

res = maxDifference(_a);
f.write(str(res) + "\n")

f.close()
